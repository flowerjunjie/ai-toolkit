# AI Toolkit v0.3.0 开发计划

## 🎯 版本主题
**真正的 AI 工具箱** - 从玩具到生产工具

## 📋 开发优先级

### P0 - 核心功能增强

#### 1. 真正的向量检索 RAG
**当前问题**: 现在只是简单的关键词匹配
**解决方案**: 集成 ChromaDB + Sentence Transformers

**任务**:
- [ ] 集成 ChromaDB
- [ ] 添加 Embedding 模型支持
- [ ] 实现向量索引
- [ ] 优化检索算法
- [ ] 添加混合检索（向量+关键词）

**技术方案**:
```python
# 使用 ChromaDB
import chromadb
from sentence_transformers import SentenceTransformer

# 创建向量数据库
client = chromadb.PersistentClient(path="./rag_db")
collection = client.create_collection("docs")

# 向量化文档
embedder = SentenceTransformer('all-MiniLM-L6-v2')
embeddings = embedder.encode(documents)

# 存储向量
collection.add(
    embeddings=embeddings,
    documents=documents,
    ids=ids
)

# 检索
results = collection.query(
    query_texts=[question],
    n_results=3
)
```

**依赖**:
```bash
pip install chromadb sentence-transformers
```

#### 2. 配置文件热重载
**当前问题**: 修改配置需要重启
**解决方案**: 使用 watchdog 监听文件变化

**任务**:
- [ ] 添加文件监听
- [ ] 自动重载配置
- [ ] 通知用户配置已更新

#### 3. 日志系统
**当前问题**: 没有日志记录
**解决方案**: 集成 Python logging

**任务**:
- [ ] 配置日志格式
- [ ] 添加文件日志
- [ ] 添加日志级别控制
- [ ] 添加日志轮转

### P1 - 用户体验提升

#### 4. 进度条优化
**当前问题**: 大文件下载没有进度
**解决方案**: 使用 Rich Progress

**任务**:
- [ ] 模型下载进度条
- [ ] RAG 索引进度条
- [ ] 文件处理进度条

#### 5. 错误处理增强
**当前问题**: 错误提示不够友好
**解决方案**: 统一错误处理和提示

**任务**:
- [ ] 自定义异常类
- [ ] 友好的错误消息
- [ ] 错误恢复建议
- [ ] 详细的调试信息

#### 6. 配置验证
**当前问题**: 配置可能无效
**解决方案**: 使用 Pydantic 验证

**任务**:
- [ ] 启动时验证配置
- [ ] 配置文件 Schema
- [ ] 自动修复无效配置

### P2 - 新功能

#### 7. 命令别名
**需求**: 简化常用命令
```bash
# alias
ai-toolkit mls = models list
ai-toolkit mrun = models run
```

#### 8. 历史记录
**需求**: 记录使用历史
```bash
ai-toolkit history
ai-toolkit history --clear
```

#### 9. 配置导入导出
**需求**: 分享配置
```bash
ai-toolkit config export my-config.json
ai-toolkit config import my-config.json
```

#### 10. 多语言支持
**需求**: 英文界面
```bash
ai-toolkit --lang en
```

---

## 🛠️ 技术债务

### 代码质量
- [ ] 添加类型注解（mypy strict）
- [ ] 提高测试覆盖率（>80%）
- [ ] 性能优化（profiling）
- [ ] 内存优化（大文件处理）

### 文档
- [ ] API 文档（Sphinx）
- [ ] 架构图
- [ ] 视频教程
- [ ] 常见问题（FAQ）

---

## 📊 v0.3.0 功能清单

### RAG 增强
- [x] ChromaDB 集成
- [x] 向量索引
- [x] 混合检索
- [x] 重排序

### 用户体验
- [x] 进度条
- [x] 日志系统
- [x] 错误处理
- [x] 配置验证

### 新功能
- [x] 命令别名
- [x] 历史记录
- [x] 配置导入导出
- [x] 多语言支持

### 质量
- [x] 测试覆盖率 >80%
- [x] 类型注解完整
- [x] 性能优化
- [x] API 文档

---

## 🎯 发布目标

- 功能完整：真正的生产工具
- 性能优秀：快速响应
- 稳定可靠：错误处理完善
- 文档齐全：易于上手

---

**预计时间**: 2-3 周
**里程碑**: ChromaDB RAG + 生产就绪
